[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "finsight-ai"
version = "1.0.0"
description = "AI-powered, privacy-first personal finance assistant"
requires-python = ">=3.9"
dependencies = [
    "pydantic>=2.0",
    "typer>=0.9",
    "rich>=13.0",
    "fastapi>=0.100",
    "uvicorn>=0.23",
]

[project.scripts]
finsight = "finsight.cli:app"